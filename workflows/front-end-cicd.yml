name: Upload website to S3

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: jakejarvis/s3-sync-action@master
    with:
      args: --acl public-read --follow-symlinks --delete
    env: 
      AWS_S3_BUCKET: ${{ alexisjvilches-resume.com }}
      AWS_ACCESS_KEY_ID: ${{ AKIAQQABDMW45LTDMFVP }}
      AWS_SECRET_ACCESS_KEY: ${{ 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 }}
      AWS_REGION: 'us-west-2'
      SOURCE_DIR: 'src'
